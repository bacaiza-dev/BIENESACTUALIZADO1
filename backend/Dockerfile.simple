FROM node:20-alpine

WORKDIR /app

# Instalar dependencias
COPY package*.json ./
RUN npm ci

# Copiar código fuente
COPY . .

# Exponer puerto
EXPOSE 3000

# Instalar curl para health check
RUN apk add --no-cache curl

# Comando de inicio
CMD ["node", "server.js"]